<li class="bookmark-list" rid="{{id}}">
    <div class="bm-dark-layer"></div>
    <div class="bookmark-photo" style="background-image: url({{thumbnail}})">
        <div class="bookmark-element">
            <div class="store-name">
                {{title}}
                <button class="btn-bookmark">
                    <i class="bookmark-btn fa fa-star"></i>
                </button>
            </div>
            <div class="bookmark-address">
                {{address}}
            </div>
            <div class="commentary-container">
                <ul class="commentary">
                    {{#each comment}}
                    <li>#{{hashtag}}</li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>
    <div class="location-info" lat="{{lat}}" lng="{{lng}}"></div>
</li>
